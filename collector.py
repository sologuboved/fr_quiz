from pymongo import MongoClient

from global_vars import (
    ADJECTIFS,
    DB_NAME,
    LOCALHOST,
    NOMS,
    PHRASES,
    PORT,
    TOUT,
    VERBES,
    VOCABULAIRES,
)


def collect(categorie):
    ...